# 系统及显卡支持

本页介绍 OrcaLab 支持的操作系统、显卡及其他硬件要求，帮助您在安装前确认环境兼容性。

## 一、支持的操作系统

### 1.1 Windows 系统

| 系统 | 支持情况 | 备注 |
| --- | --- | --- |
| Windows 11 | ✅ 官方推荐 | 性能与稳定性最佳 |
| Windows 10 | ✅ 支持 | 需保持最新系统更新 |

### 1.2 Linux 系统

| 系统 | 支持情况 | 备注 |
| --- | --- | --- |
| Ubuntu 22.04 LTS | ✅ 官方推荐 | 稳定性高，兼容性最佳 |
| Ubuntu 24.04 LTS | ✅ 支持 | 较新版本，硬件支持更好 |
| 统信 UOS V20 | ✅ 支持 | 国产系统适配 |

### 1.3 系统架构要求

```
x86_64 (64位) 架构
```

### 1.4 不支持的操作系统

- ❌ macOS（Intel / Apple Silicon 均不支持）
- ❌ Windows 10 以下系统（未官方测试）
- ❌ Windows Server（不支持）
- ❌ WSL（Windows Subsystem for Linux）支持度有限
- ❌ CentOS / RHEL / Fedora（未官方测试）
- ❌ Debian（可能兼容但未官方支持）

---

## 二、显卡支持

### 2.1 NVIDIA RTX 系列（推荐）

OrcaLab 推荐使用 NVIDIA RTX 系列显卡，**需 RTX 3060 及以上型号**。

| 显卡系列 | 支持型号 | 建议驱动版本 | 备注 |
| --- | --- | --- | --- |
| RTX 50 系列 | RTX 5090、5080、5070 等 | ≥ 550.00 | 5080/5070 已在 Windows 11 验证，5090 等同架构型号理论兼容但未测试 |
| RTX 40 系列 | RTX 4090、4080、4070、4060 等 | ≥ 535.00 | 4080(Win11)、4070(Win11/Ubuntu 22.04/24.04)、4060(Ubuntu 24.04) 已验证，4090 等未测试 |
| RTX 30 系列 | RTX 3090、3080、3070、3060 | ≥ 470.00 | ⚠️ 仅 3090(Windows 10) 已验证，3080/3070/3060 理论兼容但未测试；30 系列高性能场景可能受限 |

> ⚠️ **注**：同系列未列出型号理论兼容但未经过官方测试，使用中如遇问题请联系技术支持。

#### 检查 NVIDIA 显卡信息

```bash
# 查看显卡型号
lspci | grep VGA

# 查看 NVIDIA 驱动版本
nvidia-smi

# 查看 CUDA 版本
nvcc --version
```

### 2.2 AMD 系列

| 显卡型号 | 支持情况 | 备注 |
| --- | --- | --- |
| AMD Radeon RX 9070 XT | ✅ 已测试 | 需 AMD-SMI 版本 26.2.1+fc0010cf6a 及以上 |
| 其他 AMD 显卡 | ❓ 未测试 | 暂未官方测试，不保证兼容性 |

#### 检查 AMD 显卡信息

```bash
# 查看 AMD 显卡信息
lspci | grep -i amd

# 查看 AMD-SMI 版本
amd-smi version
```

---

## 三、硬件要求

### 3.1 推荐配置

| 组件 | 要求 |
| --- | --- |
| CPU | Intel i7 / AMD Ryzen 7 以上 |
| 内存 | 32GB 以上 |
| 硬盘 | 100GB+ 可用空间（用于资产存储） |
| 显卡 | NVIDIA RTX 40/50 系列（已验证：4060/4070/4080/5070/5080）或 AMD Radeon RX 9070 XT |

### 3.2 最低配置

| 组件 | 要求 |
| --- | --- |
| CPU | Intel i5 / AMD Ryzen 5 |
| 内存 | 8GB |
| 硬盘 | 50GB+ 可用空间 |
| 显卡 | NVIDIA RTX 4060 及以上（已验证）；RTX 3060-3080 理论兼容但未测试；AMD Radeon RX 9070 XT |

---

## 四、系统环境要求

### 4.1 Python 环境

```bash
# 通过 Miniconda 管理
Python 3.12 (推荐版本)
```

### 4.2 系统库依赖（Linux）

```bash
# 必需的系统库
libx265-dev          # 视频编解码库
build-essential      # 编译工具链
```

### 4.3 网络要求

- 稳定的网络连接用于下载资产
- 能够访问 PyPI 镜像源
- 能够访问资产库服务

### 4.4 权限要求

```bash
# Linux 需要 sudo 权限安装系统依赖
sudo apt update
sudo apt install libx265-dev
```

```bash
# Windows 需要 CMD 管理员权限
```

---

## 五、安装前检查

### 5.1 系统兼容性检查

```bash
# 检查系统版本
lsb_release -a            # Linux
winver                    # Windows

# 检查系统架构
uname -m                  # 应显示 x86_64

# 检查可用空间
df -h                     # Linux

# 检查内存
free -h                   # Linux
```

### 5.2 环境准备检查清单

- [ ] Windows 10/11 或 Ubuntu 22.04/24.04 LTS 或 统信 UOS V20
- [ ] x86_64 架构
- [ ] NVIDIA RTX 4060 及以上显卡（已验证：3090/4060/4070/4080/5070/5080）或 AMD Radeon RX 9070 XT
- [ ] 合适的显卡驱动版本
- [ ] sudo / 管理员权限
- [ ] 网络连接稳定
- [ ] 充足的存储空间

---

## 六、虚拟化环境说明

### 6.1 虚拟机使用

> ⚠️ **不推荐在虚拟机中运行**
> - GPU 直通配置复杂
> - 性能损失显著
> - 可能出现兼容性问题

如需远程连接虚拟机，推荐使用 **Sunshine + Moonlight** 游戏级性能的远程桌面进行连接。

### 6.2 容器化支持

Docker 支持有限：
- 需要 GPU 容器运行时
- 图形界面配置复杂
- 建议直接物理机安装

---

## 七、相关链接

- [Linux 上安装 ORCA Lab 指南](环境准备/Ubuntu系统安装OrcaLab指南_v1.0.md)
- [Windows 上安装 ORCA Lab 指南](环境准备/Windows系统安装OrcaLab指南_v1.0.md)
- [常见问题：OrcaLab 支持哪些操作系统](FAQ-list/005-orcalab支持哪些操作系统.md)
